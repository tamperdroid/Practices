MySQLdb

try:
	db = MySQLdb.connect(Host,userName,pwd,Database)

	#cursor object
	cursor=db.cursor()
	cursor.fetchall()
	cursor.execute(sql)

	db.commit()
	db.roleback()
	db.close()
except Warning,Error,InterfaceError,DatabaseError,DataError,OperationalError,IntegrityError,InternalError,ProgrammingError,NotSupportedError as e:
	print e
	
	


range(0, 20, 2)
range(start,stop,step)
10 in range(0, 20, 2)
 True
 
 if val is None:
     print "None"
  